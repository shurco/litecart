{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "cart",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd",
      "args": ["serve", "--dev"]
      //"args": ["init"]
      //"args": ["update"]
      //"args": ["migrate"]
      //"args": ["serve", "--no-site"]
      //"args": ["serve"]
      //"args": ["serve", "--help"]
    }
  ]
}
